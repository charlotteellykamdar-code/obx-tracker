{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": ["python"],
      "buildCommand": "pip install requests beautifulsoup4 playwright && playwright install chromium"
    }
  },
  "deploy": {
    "startCommand": "python tracker.py",
    "restartPolicyType": "ON_FAILURE"
  }
}
